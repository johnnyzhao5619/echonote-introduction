/**
 * @deprecated 请改用 '@/config/app'。
 * 为兼容历史代码保留的导出，后续可以逐步移除。
 */
export { APP_CONFIG } from './app'
export type { AppConfig, ImageContext, ImageFormat, SupportedLocale } from './app'
export { APP_CONFIG as default } from './app'
